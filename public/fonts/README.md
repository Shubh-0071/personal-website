# Fonts Directory

Drop your custom font file **`Overy.ttf`** (or `Overy.woff` / `Overy.woff2`) directly into this directory.

The website is pre-configured in `src/app/globals.css` to load this local font and fall back gracefully to Google Font **"Silkscreen"** if it is not present.
